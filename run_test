# This is a script that is convenient to compile and execute tests.
printf "1. Build/Rebuild the executable file:\n" &&
make && printf "\n" &&
printf "2. Executing test:\n" &&
./virtual_display_util_test
